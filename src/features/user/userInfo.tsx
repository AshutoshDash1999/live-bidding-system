import { useSelector, useDispatch } from 'react-redux'
import { updateUserName, updateUserEmail, updateUserRole } from "./userSlice";

function userInfo() {
  return (
    <div>userInfo</div>
  )
}
export default userInfo